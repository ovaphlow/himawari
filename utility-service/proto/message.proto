syntax = "proto3";

package miscdata;

service Message {
    rpc Save (SaveRequest) returns (Reply) {}
}

message SaveRequest {
    string uuid = 1;
    int32 user_id = 2;
    string doc = 3;
}

message Reply {
    string data = 1;
}
